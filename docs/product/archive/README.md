# Product Archive

이 폴더는 현재 앱 컴파일 경로에서 쓰지 않는 디자인 참고, 이전 요청서, UI 초안을 보관합니다.

- `FRONTEND_DESIGN_REFERENCES.md`: 이전 프론트엔드 디자인 참고 자료
- `FRONTEND_REDESIGN_REQUEST.md`: 이전 리디자인 요청서
- `livedock-landing-components/`: `frontend/components/livedock/landing/`에서 사용하지 않던 랜딩 컴포넌트 초안

실제 서비스 UI는 `frontend/` 아래에서 import되는 파일만 기준으로 봅니다.
